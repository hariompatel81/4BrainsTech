let length = prompt("Enter length (in cm) :");
let perimeter = prompt("Enter perimeter (in cm):");
let breadth = (perimeter/2) - length;
console.log(breadth);
let area = length*breadth;
console.log(area);
